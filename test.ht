// let initial = 3;

// if (initial == 1 and true) {r
//     print("Initial equals one!");
// } else if (initial == 2) {
//     print("Initial equals two!!");
// } else if (initial == 3) {
//     print("Initial equals three!!");
// } else {
//     print("Initial equals this value: '" + initial + "'.");
// }

// let x = 0;

// while (x <= 10) {
//     print("X equals " + x);
//     x = x + 1;
// }

// print(false ? "value 1" : true ? "value 2" : "value 3");

// for await x in myArray #loop {

// }

// let a = [1, 1 + 1, false ? "value 1" : true ? "value 2" : "value 3"];
// print(a);

let a = 1;

print(a--); // 1
print(a);   // 0