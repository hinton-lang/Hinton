import { NeuralNetwork, Layer, Activations } from 'sdtlib@ML'

const Neural_Network = new NeuralNetwork(
    input_size = 748,
    output_size = 10,
    hidden_layers = [
        new Layer(16, Activations.Sigmoid),
        new Layer(8, Activations.ReLu),
        new Layer(16, Activations.ReLu),
    ],
    learning_rate = 0.6
);

// Trains the Neural Network
await Neural_Network.train(data);

// Makes a prediction
let output = await Neural_Network.predict(some_input)
print(output);